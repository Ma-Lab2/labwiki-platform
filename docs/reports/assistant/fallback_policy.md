# Fallback Policy

- 主模型：`claude-sonnet-4-5-20250929-thinking`
- 降级模型：`gemini-3.1-pro-preview-all`
- 规则 1：默认先走主模型。
- 规则 2：主模型失败或超时后，立即切换到降级模型重试一次。
- 规则 3：降级模型仍失败时，返回结构化错误，不继续无限重试。
- 规则 4：深度调研模型不进入实时 loop，只做异步专项任务。
- 规则 5：审核类高价值任务后续可切到 `claude-opus-4-6`，但不在这份脚本中默认执行。
